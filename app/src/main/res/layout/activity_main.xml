<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/imgIndia"
        android:src="@drawable/in_flag"
        android:layout_width="200px"
        android:layout_height="200px"/>

    <ImageView
        android:id="@+id/imgCanada"
        android:layout_marginTop="20px"
        android:src="@drawable/flag_canda"
        android:layout_width="200px"
        android:layout_height="200px"/>

    <TextView
        android:id="@+id/txtLabelEnterName"
        android:text="@string/label_enter_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <EditText
        android:id="@+id/edtName"
        android:hint="e.g. Vishal Jagtap"
        android:inputType="text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/btnSubmit"
        android:text="@string/label_btn_submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/txtName"
        android:textSize="30sp"
        android:background="#DDCDEE"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
</LinearLayout>

